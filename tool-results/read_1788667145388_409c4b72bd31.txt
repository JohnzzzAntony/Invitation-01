     1→     1→/* ==========================================================================
     2→     2→   Ever RSVP — event website builder (step 3)
     3→     3→   Left: every field of the reference format (hero, countdown & details,
     4→     4→   story, gallery, events, venue, RSVP, contact) + full design controls.
     5→     5→   Right: live preview rendered by templates.js.
     6→     6→   ========================================================================== */
     7→     7→(function () {
     8→     8→  'use strict';
     9→     9→
    10→    10→  var FLOW_KEY = 'ever-rsvp-flow';
    11→    11→  var EVENT_KEY = 'ever-rsvp-event';
    12→    12→
    13→    13→  /* ------------------------------------------------------------------ *
    14→    14→   *  Section + field catalogue                                          *
    15→    15→   * ------------------------------------------------------------------ */
    16→    16→  var SECTIONS = [
    17→    17→    { id: 'hero', label: 'Hero',
    18→    18→      fields: [
    19→    19→        { k: 'kicker1', label: 'Kicker line 1', type: 'text', ph: "You're invited to" },
    20→    20→        { k: 'kicker2', label: 'Kicker line 2', type: 'text', ph: 'the wedding of' },
    21→    21→        { k: 'photo',   label: 'Hero photo', type: 'photo' },
    22→    22→        { k: 'btnText', label: 'Button text', type: 'text', ph: 'Enter invitation' }
    23→    23→      ] },
    24→    24→    { id: 'countdown', label: 'Countdown & details',
    25→    25→      fields: [
    26→    26→        { k: 'label', label: 'Countdown label', type: 'text', ph: 'Countdown to our big day' },
    27→    27→        { k: 'quote', label: 'Quote under the timer', type: 'text', ph: 'Two hearts, one love…' }
    28→    28→      ] },
    29→    29→    { id: 'story', label: 'Our story',
    30→    30→      fields: [ { k: 'title', label: 'Section title', type: 'text', ph: 'Our story' } ],
    31→    31→      list: { k: 'items', label: 'Milestone', blank: function () {
    32→    32→        return { photo: 'couple', title: 'New milestone', date: '', text: '' }; },
    33→    33→        fields: [
    34→    34→          { k: 'photo', label: 'Photo', type: 'photo' },
    35→    35→          { k: 'title', label: 'Title', type: 'text', ph: 'First met' },
    36→    36→          { k: 'date',  label: 'Date line', type: 'text', ph: '14 March 2021' },
    37→    37→          { k: 'text',  label: 'Description', type: 'text', ph: 'A chance meeting…' }
    38→    38→        ] } },
    39→    39→    { id: 'gallery', label: 'Gallery',
    40→    40→      fields: [
    41→    41→        { k: 'title', label: 'Section title', type: 'text', ph: 'Gallery' },
    42→    42→        { k: 'btn',   label: 'Button text (empty = hide)', type: 'text', ph: 'View more photos' }
    43→    43→      ],
    44→    44→      list: { k: 'items', label: 'Photo', blank: function () { return { src: 'decor' }; },
    45→    45→        fields: [ { k: 'src', label: 'Photo', type: 'photo' } ] } },
    46→    46→    { id: 'events', label: 'Events',
    47→    47→      fields: [ { k: 'title', label: 'Section title', type: 'text', ph: 'Events' } ],
    48→    48→      list: { k: 'items', label: 'Event', blank: function () {
    49→    49→        return { icon: 'rings', name: 'New event', time: '', venue: '' }; },
    50→    50→        fields: [
    51→    51→          { k: 'icon',  label: 'Icon', type: 'icon' },
    52→    52→          { k: 'name',  label: 'Event name', type: 'text', ph: 'Wedding ceremony' },
    53→    53→          { k: 'time',  label: 'Time', type: 'text', ph: '05:00 PM - 06:30 PM' },
    54→    54→          { k: 'venue', label: 'Venue / hall', type: 'text', ph: 'The Grand Palms · Main lawn' }
    55→    55→        ] } },
    56→    56→    { id: 'venue', label: 'Venue & map',
    57→    57→      fields: [
    58→    58→        { k: 'name',    label: 'Venue name', type: 'text', ph: 'The Grand Palms' },
    59→    59→        { k: 'address', label: 'Full address', type: 'text', ph: 'No. 123, Palm Avenue…' },
    60→    60→        { k: 'dirUrl',  label: 'Directions link (optional)', type: 'text', ph: 'https://maps.google.com/…' },
    61→    61→        { k: 'mapUrl',  label: 'Map embed URL (optional)', type: 'text', ph: 'https://www.openstreetmap.org/…' }
    62→    62→      ],
    63→    63→      list: { k: 'items', label: 'Amenity', blank: function () { return { icon: 'heart', label: 'New amenity' }; },
    64→    64→        fields: [
    65→    65→          { k: 'icon',  label: 'Icon', type: 'icon' },
    66→    66→          { k: 'label', label: 'Label', type: 'text', ph: 'Ample parking' }
    67→    67→        ] } },
    68→    68→    { id: 'rsvp', label: 'RSVP form',
    69→    69→      fields: [
    70→    70→        { k: 'title',       label: 'Heading', type: 'text', ph: 'Kindly RSVP' },
    71→    71→        { k: 'note',        label: 'Note before date', type: 'text', ph: 'Please confirm your presence by' },
    72→    72→        { k: 'deadline',    label: 'Reply deadline', type: 'date' },
    73→    73→        { k: 'showPhone',   label: 'Ask for phone number', type: 'check' },
    74→    74→        { k: 'guestsMax',   label: 'Max guests per reply', type: 'number', min: 1, max: 12 },
    75→    75→        { k: 'showMsg',     label: 'Include message box', type: 'check' },
    76→    76→        { k: 'submit',      label: 'Submit button text', type: 'text', ph: 'Submit RSVP' },
    77→    77→        { k: 'success',     label: 'Success message', type: 'text', ph: 'Thank you!' }
    78→    78→      ],
    79→    79→      list: { k: 'meals', label: 'Meal option', blank: function () { return ''; },
    80→    80→        fields: [ { k: '', label: 'Option', type: 'text', ph: 'Vegetarian' } ] } },
    81→    81→    { id: 'contact', label: 'Contact & footer',
    82→    82→      fields: [
    83→    83→        { k: 'phone',     label: 'Phone', type: 'text', ph: '+91 98765 43210' },
    84→    84→        { k: 'email',     label: 'E-mail', type: 'text', ph: 'hello@example.com' },
    85→    85→        { k: 'whatsapp',  label: 'WhatsApp link text', type: 'text', ph: 'WhatsApp us' },
    86→    86→        { k: 'thanks',    label: 'Thank-you line', type: 'text', ph: 'Thank you for being a part…' },
    87→    87→        { k: 'copyright', label: 'Copyright extra (optional)', type: 'text', ph: '' },
    88→    88→        { k: 'credit',    label: 'Credit line', type: 'text', ph: 'Made with ♥ for our big day' },
    89→    89→        { k: 'social.wa',    label: 'WhatsApp icon', type: 'check' },
    90→    90→        { k: 'social.fb',    label: 'Facebook icon', type: 'check' },
    91→    91→        { k: 'social.ig',    label: 'Instagram icon', type: 'check' },
    92→    92→        { k: 'social.share', label: 'Share icon', type: 'check' }
    93→    93→      ] }
    94→    94→  ];
    95→    95→
    96→    96→  var TEMPLATE_MAP = { /* old v1/v2 ids → new themes */
    97→    97→    eucalyptus: 'sage', blush: 'blush', monogram: 'champagne', 'golden-hour': 'champagne',
    98→    98→    midnight: 'navy', 'garden-party': 'sage', ocean: 'navy', terracotta: 'terracotta', ivory: 'champagne'
    99→    99→  };
   100→   100→  var FONT_MAP = { classic: 'corm', romantic: 'paris', modern: 'jost' };
   101→   101→
   102→   102→  /* ------------------------------------------------------------------ *
   103→   103→   *  State                                                              *
   104→   104→   * ------------------------------------------------------------------ */
   105→   105→  function defaults() { return window.EVER_siteDefaults(); }
   106→   106→
   107→   107→  var state = defaults();
   108→   108→  var ui = { open: {} };
   109→   109→  var pvTimer = null;
   110→   110→
   111→   111→  function readJson(key) {
   112→   112→    try { return JSON.parse(localStorage.getItem(key) || 'null'); }
   113→   113→    catch (e) { return null; }
   114→   114→  }
   115→   115→  function writeJson(key, val) {
   116→   116→    try { localStorage.setItem(key, JSON.stringify(val)); } catch (e) { /* ignore */ }
   117→   117→  }
   118→   118→  function esc(s) { return window.EVER_esc(s); }
   119→   119→  function slugify(s) {
   120→   120→    return String(s || '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'your-event';
   121→   121→  }
   122→   122→  function deepMerge(dst, src) {
   123→   123→    if (!src || typeof src !== 'object') return dst;
   124→   124→    for (var k in src) {
   125→   125→      if (!Object.prototype.hasOwnProperty.call(src, k)) continue;
   126→   126→      if (src[k] && typeof src[k] === 'object' && !Array.isArray(src[k]) && dst[k] && typeof dst[k] === 'object' && !Array.isArray(dst[k])) {
   127→   127→        deepMerge(dst[k], src[k]);
   128→   128→      } else if (src[k] !== undefined) {
   129→   129→        dst[k] = src[k];
   130→   130→      }
   131→   131→    }
   132→   132→    return dst;
   133→   133→  }
   134→   134→
   135→   135→  function loadState() {
   136→   136→    var d = defaults();
   137→   137→    var flow = readJson(FLOW_KEY) || {};
   138→   138→    if (flow.design) d.templateId = flow.design;
   139→   139→    var saved = readJson(EVENT_KEY);
   140→   140→    if (saved && saved.v === 3) {
   141→   141→      deepMerge(d, saved);
   142→   142→      if (saved.order && saved.order.length === d.order.length) d.order = saved.order.slice();
   143→   143→    } else if (saved) {
   144→   144→      /* v1/v2 migration — carry what still applies */
   145→   145→      var id = TEMPLATE_MAP[saved.templateId] || saved.templateId;
   146→   146→      if (id) d.templateId = id;
   147→   147→      if (saved.fontPair && FONT_MAP[saved.fontPair]) d.nameFont = FONT_MAP[saved.fontPair];
   148→   148→      ['accent', 'btnShape', 'spacing'].forEach(function (k) {
   149→   149→        if (saved[k]) d[k] = saved[k];
   150→   150→      });
   151→   151→      ['nameA', 'nameB', 'date', 'venue', 'city'].forEach(function (k) {
   152→   152→        if (saved[k]) d.basics[k] = saved[k];
   153→   153→      });
   154→   154→      if (saved.sections) {
   155→   155→        if (saved.sections.schedule && saved.sections.schedule.items) {
   156→   156→          d.sections.events.items = saved.sections.schedule.items.map(function (it) {
   157→   157→            return { icon: 'rings', name: it.title || 'Event', time: it.time || '', venue: it.desc || '' };
   158→   158→          });
   159→   159→        }
   160→   160→        if (saved.sections.rsvp && saved.sections.rsvp.deadline) {
   161→   161→          d.sections.rsvp.deadline = saved.sections.rsvp.deadline;
   162→   162→        }
   163→   163→        if (saved.sections.contact) {
   164→   164→          d.sections.contact.email = saved.sections.contact.email || d.sections.contact.email;
   165→   165→          d.sections.contact.phone = saved.sections.contact.phone || d.sections.contact.phone;
   166→   166→        }
   167→   167→      }
   168→   168→    }
   169→   169→    if (!window.EVER_findTemplate(d.templateId)) d.templateId = 'emerald';
   170→   170→    state = d;
   171→   171→  }
   172→   172→
   173→   173→  var saveTimer = null;
   174→   174→  function autosave() {
   175→   175→    clearTimeout(saveTimer);
   176→   176→    saveTimer = setTimeout(function () { writeJson(EVENT_KEY, state); }, 350);
   177→   177→  }
   178→   178→  function saveNow() {
   179→   179→    clearTimeout(saveTimer);
   180→   180→    writeJson(EVENT_KEY, state);
   181→   181→  }
   182→   182→
   183→   183→  /* ------------------------------------------------------------------ *
   184→   184→   *  Preview                                                            *
   185→   185→   * ------------------------------------------------------------------ */
   186→   186→  function render() {
   187→   187→    var canvas = document.getElementById('canvas');
   188→   188→    if (!canvas || !window.EVER_renderSite) return;
   189→   189→    canvas.innerHTML = '';
   190→   190→    var site = window.EVER_renderSite(state, { interactive: true });
   191→   191→    site.__wsData = state;
   192→   192→    canvas.appendChild(site);
   193→   193→    window.EVER_bindSite(site);
   194→   194→    window.EVER_tickCountdowns(site);
   195→   195→    clearInterval(pvTimer);
   196→   196→    pvTimer = setInterval(function () { window.EVER_tickCountdowns(canvas); }, 1000);
   197→   197→  }
   198→   198→
   199→   199→  function refresh() {
   200→   200→    render();
   201→   201→    autosave();
   202→   202→    /* keep flow.design in step for checkout/summary consistency */
   203→   203→    var flow = readJson(FLOW_KEY) || {};
   204→   204→    if (flow.design !== state.templateId) {
   205→   205→      flow.design = state.templateId;
   206→   206→      writeJson(FLOW_KEY, flow);
   207→   207→    }
   208→   208→  }
   209→   209→
   210→   210→  /* ------------------------------------------------------------------ *
   211→   211→   *  Sidebar — Basics                                                   *
   212→   212→   * ------------------------------------------------------------------ */
   213→   213→  function bindBasics() {
   214→   214→    document.querySelectorAll('[data-sec="_basic"]').forEach(function (input) {
   215→   215→      var k = input.getAttribute('data-k');
   216→   216→      input.value = state.basics[k] || '';
   217→   217→      input.addEventListener('input', function () {
   218→   218→        state.basics[k] = input.value;
   219→   219→        refresh();
   220→   220→      });
   221→   221→    });
   222→   222→  }
   223→   223→
   224→   224→  /* ------------------------------------------------------------------ *
   225→   225→   *  Field renderers                                                    *
   226→   226→   * ------------------------------------------------------------------ */
   227→   227→  function fieldHtml(f, value, onInput) {
   228→   228→    var wrap = document.createElement('div');
   229→   229→    wrap.className = 'ed-field' + (f.type === 'check' ? ' ed-check' : '');
   230→   230→    var id = 'f-' + Math.random().toString(36).slice(2, 8);
   231→   231→    if (f.type === 'check') {
   232→   232→      wrap.innerHTML = '<input type="checkbox" id="' + id + '"' + (value ? ' checked' : '') + '/>' +
   233→   233→        '<label for="' + id + '">' + esc(f.label) + '</label>';
   234→   234→      wrap.querySelector('input').addEventListener('change', function (e) { onInput(e.target.checked); });
   235→   235→      return wrap;
   236→   236→    }
   237→   237→    var label = '<label for="' + id + '">' + esc(f.label) + '</label>';
   238→   238→    var ctl;
   239→   239→    if (f.type === 'date') {
   240→   240→      ctl = '<input id="' + id + '" type="date" value="' + esc(value) + '"/>';
   241→   241→    } else if (f.type === 'number') {
   242→   242→      ctl = '<input id="' + id + '" type="number" min="' + (f.min || 1) + '" max="' + (f.max || 12) + '" value="' + esc(value) + '"/>';
   243→   243→    } else if (f.type === 'photo') {
   244→   244→      var opts = '<option value="">Choose photo…</option>';
   245→   245→      window.EVER_PHOTOS.forEach(function (p) {
   246→   246→        opts += '<option value="' + p.id + '"' + (value === p.id ? ' selected' : '') + '>' + esc(p.label) + '</option>';
   247→   247→      });
   248→   248→      var isUrl = value && String(value).indexOf('/') > -1;
   249→   249→      ctl = '<div class="ed-photo-row">' +
   250→   250→          '<select data-role="pick">' + opts + '</select>' +
   251→   251→          '<input type="text" data-role="url" placeholder="…or paste image URL" value="' + (isUrl ? esc(value) : '') + '"/>' +
   252→   252→        '</div>';
   253→   253→    } else if (f.type === 'icon') {
   254→   254→      var labels = { rings: 'Rings', cocktail: 'Glasses', dinner: 'Dinner', music: 'Music', calendar: 'Calendar',
   255→   255→        clock: 'Clock', pin: 'Pin', dress: 'Dress', car: 'Parking', bell: 'Bell', chair: 'Accessible',
   256→   256→        snow: 'Cooling', phone: 'Phone', mail: 'Mail', whatsapp: 'WhatsApp', facebook: 'Facebook',
   257→   257→        instagram: 'Instagram', share: 'Share', heart: 'Heart', camera: 'Camera' };
   258→   258→      var iopts = '';
   259→   259→      Object.keys(window.EVER_ICONS).forEach(function (k) {
   260→   260→        iopts += '<option value="' + k + '"' + (value === k ? ' selected' : '') + '>' + (labels[k] || k) + '</option>';
   261→   261→      });
   262→   262→      ctl = '<select data-role="icon">' + iopts + '</select>';
   263→   263→    } else {
   264→   264→      ctl = '<input id="' + id + '" type="text" placeholder="' + esc(f.ph || '') + '" value="' + esc(value) + '"/>';
   265→   265→    }
   266→   266→    wrap.innerHTML = label + ctl;
   267→   267→
   268→   268→    if (f.type === 'photo') {
   269→   269→      var pick = wrap.querySelector('[data-role="pick"]');
   270→   270→      var url = wrap.querySelector('[data-role="url"]');
   271→   271→      pick.addEventListener('change', function () {
   272→   272→        if (!pick.value) return;
   273→   273→        url.value = '';
   274→   274→        onInput(pick.value);
   275→   275→      });
   276→   276→      url.addEventListener('input', function () {
   277→   277→        onInput(url.value.trim());
   278→   278→      });
   279→   279→    } else if (f.type === 'icon') {
   280→   280→      wrap.querySelector('select').addEventListener('change', function (e) { onInput(e.target.value); });
   281→   281→    } else if (f.type !== 'check') {
   282→   282→      var txt = wrap.querySelector('input');
   283→   283→      txt.addEventListener('input', function (e) { onInput(e.target.value); });
   284→   284→      /* UX guard for link fields: flag anything that is not an http(s) URL.
   285→   285→         (The renderer also enforces this — see safeUrl() in templates.js.) */
   286→   286→      if (/Url$/.test(f.k || '')) {
   287→   287→        txt.addEventListener('blur', function () {
   288→   288→          var v = txt.value.trim();
   289→   289→          var bad = v !== '' && !/^(https?:\/\/|#[a-z0-9_-]*)/i.test(v);
   290→   290→          txt.classList.toggle('ed-invalid', bad);
   291→   291→          txt.title = bad ? 'Use a full link starting with https://' : '';
   292→   292→        });
   293→   293→      }
   294→   294→    }
   295→   295→    return wrap;
   296→   296→  }
   297→   297→
   298→   298→  function getVal(obj, path) {
   299→   299→    return path.split('.').reduce(function (o, k) { return o ? o[k] : undefined; }, obj);
   300→   300→  }
   301→   301→  function setVal(obj, path, val) {
   302→   302→    var ks = path.split('.');
   303→   303→    var o = obj;
   304→   304→    for (var i = 0; i < ks.length - 1; i++) {
   305→   305→      if (!o[ks[i]] || typeof o[ks[i]] !== 'object') o[ks[i]] = {};
   306→   306→      o = o[ks[i]];
   307→   307→    }
   308→   308→    o[ks[ks.length - 1]] = val;
   309→   309→  }
   310→   310→
   311→   311→  /* ------------------------------------------------------------------ *
   312→   312→   *  Sidebar — section groups                                           *
   313→   313→   * ------------------------------------------------------------------ */
   314→   314→  function buildGroups() {
   315→   315→    var host = document.getElementById('sec-groups');
   316→   316→    if (!host) return;
   317→   317→    host.innerHTML = '';
   318→   318→    var order = state.order && state.order.length ? state.order : SECTIONS.map(function (s) { return s.id; });
   319→   319→
   320→   320→    order.forEach(function (sid) {
   321→   321→      var meta = null;
   322→   322→      SECTIONS.forEach(function (s) { if (s.id === sid) meta = s; });
   323→   323→      if (!meta) return;
   324→   324→      var sec = state.sections[sid];
   325→   325→
   326→   326→      var det = document.createElement('details');
   327→   327→      det.className = 'ed-group' + (ui.open[sid] ? ' open' : '') + (sec.on ? '' : ' sec-off');
   328→   328→      if (ui.open[sid]) det.open = true;
   329→   329→
   330→   330→      var sum = document.createElement('summary');
   331→   331→      sum.innerHTML = '<span>' + esc(meta.label) + '</span>' +
   332→   332→        '<span class="sec-tools">' +
   333→   333→          '<button type="button" data-tool="up" aria-label="Move ' + esc(meta.label) + ' up">&#8593;</button>' +
   334→   334→          '<button type="button" data-tool="down" aria-label="Move ' + esc(meta.label) + ' down">&#8595;</button>' +
   335→   335→          '<button type="button" data-tool="eye" aria-label="Show or hide ' + esc(meta.label) + '" aria-pressed="' + sec.on + '">&#128065;</button>' +
   336→   336→        '</span>';
   337→   337→
   338→   338→      var body = document.createElement('div');
   339→   339→      body.className = 'ed-body';
   340→   340→
   341→   341→      meta.fields.forEach(function (f) {
   342→   342→        body.appendChild(fieldHtml(f, getVal(sec, f.k), function (v) {
   343→   343→          setVal(sec, f.k, v);
   344→   344→          refresh();
   345→   345→        }));
   346→   346→      });
   347→   347→
   348→   348→      if (meta.list) buildList(body, meta, sec);
   349→   349→
   350→   350→      /* tool buttons */
   351→   351→      sum.querySelectorAll('[data-tool]').forEach(function (btn) {
   352→   352→        btn.addEventListener('click', function (e) {
   353→   353→          e.preventDefault();
   354→   354→          e.stopPropagation();
   355→   355→          var tool = btn.getAttribute('data-tool');
   356→   356→          var i = state.order.indexOf(sid);
   357→   357→          if (tool === 'eye') {
   358→   358→            sec.on = !sec.on;
   359→   359→            det.classList.toggle('sec-off', !sec.on);
   360→   360→            btn.setAttribute('aria-pressed', String(sec.on));
   361→   361→          } else if (tool === 'up' && i > 0) {
   362→   362→            state.order.splice(i, 1);
   363→   363→            state.order.splice(i - 1, 0, sid);
   364→   364→            buildGroups();
   365→   365→          } else if (tool === 'down' && i < state.order.length - 1) {
   366→   366→            state.order.splice(i, 1);
   367→   367→            state.order.splice(i + 1, 0, sid);
   368→   368→            buildGroups();
   369→   369→          }
   370→   370→          refresh();
   371→   371→        });
   372→   372→      });
   373→   373→      det.addEventListener('toggle', function () { ui.open[sid] = det.open; });
   374→   374→
   375→   375→      det.appendChild(sum);
   376→   376→      det.appendChild(body);
   377→   377→      host.appendChild(det);
   378→   378→    });
   379→   379→  }
   380→   380→
   381→   381→  function buildList(body, meta, sec) {
   382→   382→    var cfg = meta.list;
   383→   383→    var arr = sec[cfg.k] = Array.isArray(sec[cfg.k]) ? sec[cfg.k] : [];
   384→   384→    var listBox = document.createElement('div');
   385→   385→    listBox.className = 'ed-list';
   386→   386→
   387→   387→    arr.forEach(function (item, idx) {
   388→   388→      var li = document.createElement('div');
   389→   389→      li.className = 'ed-li';
   390→   390→      var head = document.createElement('div');
   391→   391→      head.className = 'ed-li-head';
   392→   392→      head.innerHTML = '<b>' + esc(cfg.label) + ' ' + (idx + 1) + '</b>' +
   393→   393→        '<span>' +
   394→   394→          '<button type="button" data-op="up" aria-label="Move up" ' + (idx === 0 ? 'disabled' : '') + '>&#8593;</button>' +
   395→   395→          '<button type="button" data-op="down" aria-label="Move down" ' + (idx === arr.length - 1 ? 'disabled' : '') + '>&#8595;</button>' +
   396→   396→          '<button type="button" data-op="del" aria-label="Remove">&#10005;</button>' +
   397→   397→        '</span>';
   398→   398→      li.appendChild(head);
   399→   399→
   400→   400→      cfg.fields.forEach(function (f) {
   401→   401→        var val = f.k ? (item[f.k] || '') : item;
   402→   402→        li.appendChild(fieldHtml(f, val, function (v) {
   403→   403→          if (f.k) item[f.k] = v; else sec[cfg.k][idx] = v;
   404→   404→          refresh();
   405→   405→        }));
   406→   406→      });
   407→   407→
   408→   408→      head.querySelectorAll('[data-op]').forEach(function (btn) {
   409→   409→        btn.addEventListener('click', function () {
   410→   410→          var op = btn.getAttribute('data-op');
   411→   411→          if (op === 'del') arr.splice(idx, 1);
   412→   412→          if (op === 'up' && idx > 0) arr.splice(idx - 1, 0, arr.splice(idx, 1)[0]);
   413→   413→          if (op === 'down' && idx < arr.length - 1) arr.splice(idx + 1, 0, arr.splice(idx, 1)[0]);
   414→   414→          buildGroups();
   415→   415→          refresh();
   416→   416→        });
   417→   417→      });
   418→   418→      listBox.appendChild(li);
   419→   419→    });
   420→   420→
   421→   421→    var add = document.createElement('button');
   422→   422→    add.type = 'button';
   423→   423→    add.className = 'ed-add';
   424→   424→    add.textContent = '+ Add ' + cfg.label.toLowerCase();
   425→   425→    add.addEventListener('click', function () {
   426→   426→      arr.push(cfg.blank());
   427→   427→      buildGroups();
   428→   428→      refresh();
   429→   429→    });
   430→   430→    listBox.appendChild(add);
   431→   431→    body.appendChild(listBox);
   432→   432→  }
   433→   433→
   434→   434→  /* ------------------------------------------------------------------ *
   435→   435→   *  Design panel                                                       *
   436→   436→   * ------------------------------------------------------------------ */
   437→   437→  function accentList() {
   438→   438→    return [
   439→   439→      { id: 'tpl', hex: null, label: 'Template' },
   440→   440→      { id: '#c9a45c', label: 'Gold' },
   441→   441→      { id: '#b98a8e', label: 'Rose' },
   442→   442→      { id: '#9a7b4f', label: 'Bronze' },
   443→   443→      { id: '#7f9c7c', label: 'Sage' },
   444→   444→      { id: '#b3704e', label: 'Clay' },
   445→   445→      { id: '#9a7bc9', label: 'Lilac' }
   446→   446→    ];
   447→   447→  }
   448→   448→
   449→   449→  function buildDesign() {
   450→   450→    /* template chips */
   451→   451→    var chips = document.getElementById('tpl-chips');
   452→   452→    chips.innerHTML = '';
   453→   453→    window.EVER_allTemplates().forEach(function (t) {
   454→   454→      var chip = document.createElement('button');
   455→   455→      chip.type = 'button';
   456→   456→      chip.className = 'tpl-chip' + (state.templateId === t.id ? ' on' : '');
   457→   457→      chip.setAttribute('role', 'radio');
   458→   458→      chip.setAttribute('aria-checked', String(state.templateId === t.id));
   459→   459→      chip.innerHTML = '<span class="tpl-swatch" style="background:' + esc(t.dark) + '">' +
   460→   460→          '<i style="background:' + esc(t.gold) + '"></i></span>' + esc(t.name) +
   461→   461→        (t.custom ? '<span class="chip-x" role="button" tabindex="0" aria-label="Edit or remove">&#9998;</span>' : '');
   462→   462→      chip.addEventListener('click', function (e) {
   463→   463→        if (e.target.closest('.chip-x')) return;
   464→   464→        state.templateId = t.id;
   465→   465→        buildDesign();
   466→   466→        refresh();
   467→   467→      });
   468→   468→      if (t.custom) {
   469→   469→        var x = chip.querySelector('.chip-x');
   470→   470→        x.addEventListener('click', function () { designerEdit(t.id); });
   471→   471→      }
   472→   472→      chips.appendChild(chip);
   473→   473→    });
   474→   474→
   475→   475→    /* "create your own" chip */
   476→   476→    var newChip = document.createElement('button');
   477→   477→    newChip.type = 'button';
   478→   478→    newChip.className = 'tpl-chip tpl-chip-new';
   479→   479→    newChip.setAttribute('data-open-designer', '');
   480→   480→    newChip.innerHTML = '<span class="tpl-swatch new">+</span> New design';
   481→   481→    newChip.__dsnOnSave = function (t) {
   482→   482→      state.templateId = t.id;
   483→   483→      buildDesign();
   484→   484→      refresh();
   485→   485→    };
   486→   486→    chips.appendChild(newChip);
   487→   487→
   488→   488→    /* name font */
   489→   489→    var fonts = document.getElementById('font-cards');
   490→   490→    fonts.innerHTML = '';
   491→   491→    var fontOpts = [{ id: '', label: 'Template' }].concat(Object.keys(window.EVER_NAME_FONTS).map(function (k) {
   492→   492→      return { id: k, label: window.EVER_NAME_FONTS[k].label };
   493→   493→    }));
   494→   494→    fontOpts.forEach(function (o) {
   495→   495→      var cls = o.id ? window.EVER_NAME_FONTS[o.id].cls : '';
   496→   496→      var sample = o.id === 'jost' ? 'AA' : 'Aa';
   497→   497→      var card = document.createElement('button');
   498→   498→      card.type = 'button';
   499→   499→      card.className = 'font-card' + (state.nameFont === o.id ? ' on' : '');
   500→   500→      card.setAttribute('role', 'radio');
   501→   501→      card.setAttribute('aria-checked', String(state.nameFont === o.id));
   502→   502→      card.innerHTML = '<span class="font-sample ' + cls + '">' + sample + '</span>' + esc(o.label);
   503→   503→      card.addEventListener('click', function () {
   504→   504→        state.nameFont = o.id;
   505→   505→        buildDesign();
   506→   506→        refresh();
   507→   507→      });
   508→   508→      fonts.appendChild(card);
   509→   509→    });
   510→   510→
   511→   511→    /* body font */
   512→   512→    var body = document.getElementById('body-cards');
   513→   513→    body.innerHTML = '';
   514→   514→    Object.keys(window.EVER_BODY_FONTS).forEach(function (k) {
   515→   515→      var card = document.createElement('button');
   516→   516→      card.type = 'button';
   517→   517→      card.className = 'font-card' + (state.bodyFont === k ? ' on' : '');
   518→   518→      card.setAttribute('role', 'radio');
   519→   519→      card.setAttribute('aria-checked', String(state.bodyFont === k));
   520→   520→      card.innerHTML = '<span class="font-sample" style="font-family:' + (k === 'jost' ? 'Jost,sans-serif' : 'Lato,sans-serif') + '">Ag</span>' +
   521→   521→        esc(window.EVER_BODY_FONTS[k].label);
   522→   522→      card.addEventListener('click', function () {
   523→   523→        state.bodyFont = k;
   524→   524→        buildDesign();
   525→   525→        refresh();
   526→   526→      });
   527→   527→      body.appendChild(card);
   528→   528→    });
   529→   529→
   530→   530→    /* accent dots + custom color */
   531→   531→    var dots = document.getElementById('color-dots');
   532→   532→    dots.innerHTML = '';
   533→   533→    accentList().forEach(function (a) {
   534→   534→      var dot = document.createElement('button');
   535→   535→      dot.type = 'button';
   536→   536→      dot.className = 'color-dot' + (state.accent === a.id ? ' on' : '');
   537→   537→      dot.style.background = a.hex || 'linear-gradient(135deg,#c9a45c,#7f9c7c,#b98a8e)';
   538→   538→      dot.setAttribute('role', 'radio');
   539→   539→      dot.setAttribute('aria-checked', String(state.accent === a.id));
   540→   540→      dot.setAttribute('aria-label', a.label + ' accent');
   541→   541→      dot.title = a.label;
   542→   542→      dot.addEventListener('click', function () {
   543→   543→        state.accent = a.id;
   544→   544→        buildDesign();
   545→   545→        refresh();
   546→   546→      });
   547→   547→      dots.appendChild(dot);
   548→   548→    });
   549→   549→    var custom = document.createElement('label');
   550→   550→    custom.className = 'color-dot color-dot-custom' + (/^#/.test(state.accent) ? ' on' : '');
   551→   551→    custom.title = 'Custom accent';
   552→   552→    custom.innerHTML = '<input type="color" value="' + (/^#/.test(state.accent) ? esc(state.accent) : '#c9a45c') + '" aria-label="Custom accent color"/>';
   553→   553→    custom.querySelector('input').addEventListener('input', function (e) {
   554→   554→      state.accent = e.target.value;
   555→   555→      buildDesign();
   556→   556→      refresh();
   557→   557→    });
   558→   558→    dots.appendChild(custom);
   559→   559→
   560→   560→    /* shape + spacing */
   561→   561→    var shapes = document.getElementById('shape-row');
   562→   562→    shapes.innerHTML = '';
   563→   563→    [['pill', 'Pill'], ['soft', 'Soft'], ['square', 'Square']].forEach(function (s) {
   564→   564→      var b = document.createElement('button');
   565→   565→      b.type = 'button';
   566→   566→      b.className = 'shape-btn' + (state.btnShape === s[0] ? ' on' : '');
   567→   567→      b.setAttribute('role', 'radio');
   568→   568→      b.setAttribute('aria-checked', String(state.btnShape === s[0]));
   569→   569→      b.innerHTML = '<span class="shape-demo sp-' + s[0] + '"></span>' + s[1];
   570→   570→      b.addEventListener('click', function () {
   571→   571→        state.btnShape = s[0];
   572→   572→        buildDesign();
   573→   573→        refresh();
   574→   574→      });
   575→   575→      shapes.appendChild(b);
   576→   576→    });
   577→   577→    var spacing = document.getElementById('ed-spacing');
   578→   578→    spacing.value = state.spacing;
   579→   579→    spacing.onchange = function () { state.spacing = spacing.value; refresh(); };
   580→   580→  }
   581→   581→
   582→   582→  function designerEdit(id) {
   583→   583→    var tpl = window.EVER_readCustomTemplates().filter(function (t) { return t.id === id; })[0];
   584→   584→    if (!tpl) return;
   585→   585→    window.EVER_openDesigner({
   586→   586→      template: tpl,
   587→   587→      onSave: function (t) {
   588→   588→        if (state.templateId === id) state.templateId = t.id;
   589→   589→        buildDesign();
   590→   590→        refresh();
   591→   591→      }
   592→   592→    });
   593→   593→  }
   594→   594→
   595→   595→  /* ------------------------------------------------------------------ *
   596→   596→   *  Tabs / device / save / publish                                     *
   597→   597→   * ------------------------------------------------------------------ */
   598→   598→  function bindChrome() {
   599→   599→    document.querySelectorAll('.ed-tab').forEach(function (tab) {
   600→   600→      tab.addEventListener('click', function () {
   601→   601→        document.querySelectorAll('.ed-tab').forEach(function (t) {
   602→   602→          t.classList.remove('on');
   603→   603→          t.setAttribute('aria-selected', 'false');
   604→   604→        });
   605→   605→        tab.classList.add('on');
   606→   606→        tab.setAttribute('aria-selected', 'true');
   607→   607→        document.querySelectorAll('.ed-panel').forEach(function (p) { p.hidden = true; });
   608→   608→        var panel = document.getElementById(tab.getAttribute('aria-controls'));
   609→   609→        if (panel) panel.hidden = false;
   610→   610→      });
   611→   611→    });
   612→   612→
   613→   613→    document.querySelectorAll('.ed-dev-btn').forEach(function (btn) {
   614→   614→      btn.addEventListener('click', function () {
   615→   615→        document.querySelectorAll('.ed-dev-btn').forEach(function (b) {
   616→   616→          b.classList.remove('on');
   617→   617→          b.setAttribute('aria-pressed', 'false');
   618→   618→        });
   619→   619→        btn.classList.add('on');
   620→   620→        btn.setAttribute('aria-pressed', 'true');
   621→   621→        document.getElementById('canvas-frame').classList.toggle('mobile', btn.getAttribute('data-dev') === 'mobile');
   622→   622→      });
   623→   623→    });
   624→   624→
   625→   625→    document.getElementById('save-btn').addEventListener('click', function () {
   626→   626→      saveNow();
   627→   627→      toast('Saved — your website lives on this device.');
   628→   628→    });
   629→   629→
   630→   630→    document.getElementById('publish-btn').addEventListener('click', function () {
   631→   631→      saveNow();
   632→   632→      var flow = readJson(FLOW_KEY) || {};
   633→   633→      var slug = slugify(state.basics.nameA + '-' + state.basics.nameB);
   634→   634→      flow.published = true;
   635→   635→      flow.slug = slug;
   636→   636→      writeJson(FLOW_KEY, flow);
   637→   637→      var badge = document.getElementById('ed-badge');
   638→   638→      badge.textContent = 'Published';
   639→   639→      badge.classList.add('published');
   640→   640→      var urlEl = document.getElementById('pub-url');
   641→   641→      if (urlEl) urlEl.textContent = slug + '.ever-rsvp.com';
   642→   642→      document.getElementById('pub-overlay').hidden = false;
   643→   643→      document.body.classList.add('modal-open');
   644→   644→    });
   645→   645→    document.getElementById('pub-close').addEventListener('click', function () {
   646→   646→      document.getElementById('pub-overlay').hidden = true;
   647→   647→      document.body.classList.remove('modal-open');
   648→   648→    });
   649→   649→    var copy = document.getElementById('copy-url');
   650→   650→    if (copy) {
   651→   651→      copy.addEventListener('click', function () {
   652→   652→        var text = document.getElementById('pub-url').textContent;
   653→   653→        var done = function () { copy.textContent = 'Copied!'; setTimeout(function () { copy.textContent = 'Copy'; }, 1600); };
   654→   654→        if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(text).then(done, done);
   655→   655→        else done();
   656→   656→      });
   657→   657→    }
   658→   658→  }
   659→   659→
   660→   660→  function toast(msg) {
   661→   661→    var t = document.getElementById('toast');
   662→   662→    if (!t) return;
   663→   663→    t.textContent = msg;
   664→   664→    t.hidden = false;
   665→   665→    clearTimeout(t.__tm);
   666→   666→    t.__tm = setTimeout(function () { t.hidden = true; }, 2600);
   667→   667→  }
   668→   668→
   669→   669→  function updateBadge() {
   670→   670→    var flow = readJson(FLOW_KEY) || {};
   671→   671→    var badge = document.getElementById('ed-badge');
   672→   672→    if (!badge) return;
   673→   673→    if (flow.published) {
   674→   674→      badge.textContent = 'Published';
   675→   675→      badge.classList.add('published');
   676→   676→    } else if (flow.order) {
   677→   677→      badge.textContent = 'Order ' + flow.order;
   678→   678→    }
   679→   679→  }
   680→   680→
   681→   681→  /* ------------------------------------------------------------------ *
   682→   682→   *  Boot                                                               *
   683→   683→   * ------------------------------------------------------------------ */
   684→   684→  loadState();
   685→   685→  bindBasics();
   686→   686→  buildGroups();
   687→   687→  buildDesign();
   688→   688→  bindChrome();
   689→   689→  updateBadge();
   690→   690→  render();
   691→   691→  window.addEventListener('beforeunload', function () { saveNow(); });
   692→   692→})();
   693→   693→