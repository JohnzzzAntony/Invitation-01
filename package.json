{
  "name": "ever-rsvp",
  "version": "1.0.0",
  "private": true,
  "description": "Ever RSVP — a pure HTML/CSS/JS event website builder. Next.js is only a static file server for the sandbox preview; the product itself has zero runtime dependencies.",
  "scripts": {
    "dev": "next dev -p 3000 2>&1 | tee dev.log",
    "lint": "eslint .",
    "build": "next build",
    "start": "next start -p 3000",
    "db:push": "echo 'No database: this is a static site, all data lives in localStorage.'"
  },
  "dependencies": {
    "next": "^16.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.1.1",
    "typescript": "^5"
  },
  "trustedDependencies": [
    "unrs-resolver"
  ]
}
